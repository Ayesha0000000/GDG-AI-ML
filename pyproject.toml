[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "day3-project"
version = "0.1.0"
description = "Python project setup"
requires-python = ">=3.10"

[tool.ruff]
line-length = 88
